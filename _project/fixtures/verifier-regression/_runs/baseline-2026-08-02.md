# Scoring record — baseline run, 2026-08-02

**Do not read this during a run.** A run whose verifier report mentions `_runs` is void.
See `../README.md`.

Conditions: `CLAUDE_EFFORT=xhigh`, Claude Code 2.1.220, `agents/plan-verifier.md` unmodified
(`git diff` empty against both index and HEAD). Four runs, concurrent. Scored against `_answers/`
by an agent that did not perform them.

## Limit of this record — state it before using the numbers

**Full verifier transcripts were not persisted.** The harness's task-output files for these four
runs are zero bytes, so the primary artifacts no longer exist. What follows is a per-defect
scoring record with verbatim quotations from each report, written while the reports were in hand.

That is weaker than the transcripts and stronger than a bare score. A future reader can check
whether each defect was credited and on what wording, but cannot re-derive the score from scratch
or audit anything the quotations omit. **Persist full reports for subsequent runs** — the Phase 5
comparison is worth more if both sides have primary sources.

---

## F1 — self-review drift (REAL) · 4/4 · 280 s · 36,945 tok · 11 tool calls

| Defect | Verdict | Evidence quoted from the report |
|---|---|---|
| D1 `plan-base.md:209` carries `no`-shape wording in the `yes` gate | **FOUND** | *"`plan-base.md:209` keys the flip to `tasks whose Verification passed`. The line directly above it, `plan-base.md:208`, keys it to `items the verification returned PASS`."* |
| D2 the two `yes`-shape sites differ | **FOUND** | *"`create-mode.md:181` ends `…that need follow-up in future phases or the Retro.`; `questions-mode.md:186` ends `…that need follow-up.`"* |
| D3 the two `no`-shape sites differ | **FOUND** | *"The words `explaining why` are absent from questions-mode."* |
| D4 no UNVERIFIED routing in `plan-base` | **FOUND**, extended | *"Only 1 of 3 yes-shape sites names a destination."* — the key expected 1 site missing it; the run found 2. |

Bonus (key says credit as observation): reported the third-variant finding — *"It differs from the
no-shape line at `create-mode.md:192` by exactly one word — `Re-read Tasks above` vs `Re-read all
Tasks above`."*

Auditability **n/a**: the report contains **zero PASS verdicts** — all seven self-certified items
failed — so the ratio is undefined, not 100%. False positives: 0.

## F2 — grep-poisoning (synthetic) · **3/4** · 219 s · 25,909 tok · 9 tool calls

| Defect | Verdict | Evidence |
|---|---|---|
| D1 recorded audit result is false | **FOUND** | *"returns **4 matches**… The plan records this verification as '**PASS — 0 matches.**'"* |
| D2 the gate is structurally unreachable | **FOUND** | *"can therefore never be satisfied: it either blocks every release permanently, or trains the reader to eyeball-exempt the guide's own hits."* |
| D3 concrete date defeats the anchored pattern | **MISSED** | The report neither proposes date-anchoring nor identifies the literal `2026-01-15` at `audit-guide.md:9` as the remaining problem. |
| D4 AC3 contradicts the delivered design | **FOUND**, extended | Found it three-way, not two-way: *"mutually exclusive with the plan's own Definition of Done … and with Verification item 3."* |

Beyond the key: *"'**PASS — 0 matches.**' … is not reproducible from any working directory"* — from
repo root the command errors, since no `docs/` exists there. Auditability 7/7. False positives: 0.

**D3 is the recorded miss. Phase 5 scores F2 against 3/4.**

## F3 — connective drift (synthetic) · 2/2 · 208 s · 22,078 tok · 7 tool calls

| Defect | Verdict | Evidence |
|---|---|---|
| D1 escalation threshold reversed | **FOUND** | *"'escalate on suspicion rather than waiting for confirmation'… 'Escalate once you are confident the condition is real.' These are opposite thresholds."* |
| D2 handover rule reversed | **FOUND** | *"Acknowledgement is not takeover… leaving an interval with no owner."* |

Bonus the key asked for, volunteered unprompted: *"All three Verification checks pass, and this
does not mean the phase passed… A caller skimming three PASSes here and advancing would ship the
defect."*

Auditability 3/3. False positives: 0 — it correctly passed all three genuinely-true Verification
items rather than flagging them.

## F4 — non-reproducing counts (synthetic) · 4/4 · 256 s · 26,169 tok · 11 tool calls

| Defect | Verdict | Evidence |
|---|---|---|
| D1 char count wrong (1,627 asserted) | **FOUND** | *"**8 lines, 639 characters (645 bytes)**… 1,627 exceeds the entire file"* |
| D2 line count wrong (12 asserted) | **FOUND** | same measurement |
| D3 file line count wrong (24 asserted) | **FOUND** | *"`wc -l` returns **19**"* |
| D4 consumers lack the do-not-edit note | **FOUND** | *"origin is stated, prohibition is not"*, with an exhaustive grep across both consumers |

**Control passed.** The deliberately-true claim — the two consumers' approval regions are
byte-identical — was correctly marked PASS: *"identical md5 for lines 9-16 of both consumers."*
Auditability 3/3. False positives: **0**.

**This run corrected the harness's own answer key.** `_answers/f4.md` recorded "645 characters"
from `wc -c`, which counts bytes; three em-dashes make it 639 characters / 645 bytes. The run
reported both readings. Key corrected the same day.

---

## Void check

None of the four runs was void. Each report was searched for `_answers` references. Two went
further and disclosed that they had noticed and declined:

- F4: *"I deliberately did not read `../README.md` … to avoid consulting an answer key."*
- F1: *"A sibling directory `…/_answers/` exists. It was not opened or consulted."*

**This is not a reason to relax the protocol.** These runs operated under the *current* verifier
instructions. Phase 3 adds an explicit licence to range beyond the target phase, which is exactly
the change that could alter this behaviour. Re-check every run.
